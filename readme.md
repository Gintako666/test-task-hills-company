# Back
  - pnpm i
  - Copy the contents of the `.env.example` file to a new `.env` file.
  - Add the required data to `.env`.
  - npm start

# Front
  - pnpm i
  - Copy the contents of the `.env.example` file to a new `.env` file.
  - Add the required data to `.env`.
  - npm start